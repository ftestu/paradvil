{
  "name": "name",
  "version": "1.0.0",
  "description": "Nuxt.js project",
  "author": "Florian Testu",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "yarn lint:js",
    "lint:js": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "heroku-postbuild": "npm install && npm run build && npm run dev"
  },
  "dependencies": {
    "@nuxt/http": "latest",
    "chart.js": "^2.9.3",
    "nuxt": "latest",
    "vue-chartjs": "^3.5.1"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^3.0.0",
    "@nuxtjs/eslint-module": "^2.0.0",
    "babel-eslint": "^10.1.0",
    "eslint": "^7.2.0",
    "eslint-plugin-nuxt": "^1.0.0"
  }
}
